/**
 * File: public-section.types.ts
 * Description: Shared section metadata types for public-page scroll and layout helpers.
 */
export type PublicSectionWithId = {
  id: string;
};

export type SectionLayout = {
  y: number;
  height: number;
};
